export interface ItemGl {
  id: string;
  alt_description: string;
  urls: {
    small: string;
    regular: string;
  };
}

export interface ModalImage {
  alt: string,
  src: string,
}

export interface ImgItem {
  id: string;
alt_description: string;
  urls: { small: string; regular: string };
}